# Dépendances du projet de prévision PAUL
# Installation : pip install -r requirements.txt

# --- Cœur (obligatoire) ---
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1          # lecture des .xlsx + export Excel multi-feuilles
scikit-learn>=1.3      # modèle GLOBAL (GBM) retenu pour ~230 produits (sinon repli Holt-Winters)
pyodbc>=5.0            # export automatique des ventes depuis la base SQL d'Elyx (PI ELECTRONIQUE)

# --- Optionnel (le pipeline fonctionne sans, en mode dégradé) ---
statsmodels>=0.14      # modèle Holt-Winters (sinon fallback décomposition)
# prophet>=1.1         # modèle Prophet (pics Ramadan/Aïd) — décommenter si besoin

# --- Tableau de bord interactif (python dashboard.py) ---
dash>=3.0              # interface web interactive (Plotly)
plotly>=5.20           # graphes interactifs
# L'onglet « Assistant » est 100 % local (aucune API, aucune dépendance réseau).

# --- Tests (python -m pytest tests/) ---
pytest>=7.0            # suite de tests automatisés
